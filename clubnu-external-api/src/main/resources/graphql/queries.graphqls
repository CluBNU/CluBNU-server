type Query {
    # 홈화면: 동아리 목록 조회
    clubs: [ClubResponse]!

    # 상세화면(프로필): 단건 조회
    club(clubId: Long!): ClubResponse!

    # 상세화면(소개): 단건 소개 조회
    clubIntroduction(clubId: Long!): ClubIntroduction!
}